    git config --global user.name "moradiyasmit" 
    git config --global user.email "moradiyasmit12@gmail.com"

    git config --global color.ui auto 

    git init
    
    git remote add origin https://github.com/moradiyasmit/demo.git

    git pull origin main 

    create a new branch 
    git checkout -b "branchname"

    switch the branch
    git checkout "branchname which need to be opend " 

    download from the git hub
    git clone url... (in cmd )

